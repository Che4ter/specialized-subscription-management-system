@model IQueryable<essentialAdmin.Models.CustomerViewModels.CustomerViewModel>

@{
    ViewData["Title"] = "Kunden Übersicht";
}

<h2>Kunden</h2>

@*@if (Model.Any())
    {
        foreach (var customer in Model)
        {
            <h3>
                <a asp-area="" asp-controller="Customer" asp-action="Edit" asp-route-id="@customer.Id">@customer.FirstName @customer.LastName</a>
            </h3>

        }
    }*@

<div class="grid mui-form">
    <div class="row">
        <div class="col-12 col-medium-12 col-small-12">
            <div class="mui-panel">
                <table id="customerTable" class="mui-table mui-table--bordered " width="100%" cellspacing="0">
                    <thead>
                        <tr>
                            <th>Id</th>  
                            <th>Vorname</th>
                            <th>Nachname</th>
                            <th>Strasse</th>
                            <th>PLZ</th>
                            <th>Ort</th>
                            <th>EMail</th>
                            <th>Edit</th>
                            <th>Delete</th>
                        </tr>
                    </thead>
                </table>  
            </div>
        </div>
    </div>
</div>

<environment include="Development">
    @section scripts {
        <script src="~/lib/DataTables/datatables.js" asp-append-version="true"></script>
    <script src="~/js/customer.js" asp-append-version="true"></script>

    }
    @section styles {
        <link rel="stylesheet" href="~/lib/DataTables/datatables.css" />
    }

</environment>

